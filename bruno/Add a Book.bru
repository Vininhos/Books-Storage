meta {
  name: Add a Book
  type: http
  seq: 3
}

post {
  url: {{host}}:8080/api/book
  body: json
  auth: inherit
}

body:json {
  {
    "Name": "A test book",
    "Author": "Vini",
    "PubYear": 2025,
    "Price": 199.99,
    "Category": "Action"
  }
}

tests {
  test("should create a book", function() {
    expect(res.getStatus()).to.equal(201);
  });
}

settings {
  encodeUrl: true
}
